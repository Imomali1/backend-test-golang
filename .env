# Server Configuration
ADDR=:8080

# Database Configuration
# Connection string format: postgresql://user:password@host:port/dbname?sslmode=disable
# For Docker: use default values below
# For local PostgreSQL: change password as needed
DB_URL=postgresql://postgres:postgres@localhost:5433/skinport_test?sslmode=disable

# Skinport API Configuration
SKINPORT_ADDR=https://api.skinport.com/v1
# Credentials are optional - API works without auth but with stricter rate limits
# Get credentials from https://skinport.com/api
SKINPORT_CLIENT_ID=
SKINPORT_CLIENT_SECRET=

# Cache Configuration (in seconds)
CACHE_TTL=300
CACHE_CLEANUP_INTERVAL=60
